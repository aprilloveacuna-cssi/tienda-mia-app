import PhaseNotice from '../components/PhaseNotice'

export default function Settings() {
  return (
    <PhaseNotice
      title="Settings"
      phase="1 (next)"
      unlocksWith="The settings table already exists (purchasing day, safety stock defaults, forecast window) — this screen just needs a form wired to it."
    />
  )
}
